apiVersion: v1
kind: Pod
metadata:
  name: insta-pod
spec:
  containers:
  - name: image-processor
    image: nginx
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
      limits:
        cpu: "200m"
        memory: "256Mi"
    ports:
    - containerPort: 80 