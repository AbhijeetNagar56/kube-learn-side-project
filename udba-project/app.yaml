# app.yaml
apiVersion: v1
kind: Pod
metadata:
  name: app
  namespace: university-accomodation-system
  labels:
    app: uas-app
spec:
  containers:
    - name: app
      image: ghcr.io/abhijeetnagar56/university-accomodation-system:latest
      imagePullPolicy: Always
      ports:
        - containerPort: 3000
      env:
        - name: SESSION_SECRET
          value: "your-secret"
        - name: ADMIN_USER
          value: "admin"
        - name: ADMIN_PASS
          value: "admin123"
        - name: DB_HOST
          value: "uas-db-service"          
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "university"
        - name: DB_USER
          value: "root"
        - name: DB_PASSWORD_FILE
          value: "/secrets/db_pass"
      volumeMounts:
        - name: db-secret-vol
          mountPath: /secrets 
          readOnly: true
      resources:
        requests:
          cpu: "250m"
          memory: "128Mi"
        limits:
          cpu: "500m"
          memory: "256Mi"
  volumes:
    - name: db-secret-vol
      secret:
        secretName: db-secret
        items:
          - key: mysql-root-password
            path: db_pass              # final path: /run/secrets/db_pass
---
apiVersion: v1
kind: Service
metadata:
  name: uas-app-service
  namespace: university-accomodation-system
spec:
  selector:
    app: uas-app
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080